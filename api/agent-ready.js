import{buildAgentReadyV1}from'../lib/agent-ready.js';
export default async function handler(req,res){res.setHeader('cache-control','no-store');res.setHeader('content-type','application/json; charset=utf-8');if(req.method!=='POST')return res.status(405).json({error:'Usa POST.'});try{const b=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{});return res.status(200).json(buildAgentReadyV1(b.report||b));}catch(e){return res.status(400).json({error:e.message||'Agent Ready analysis failed.'});}}
